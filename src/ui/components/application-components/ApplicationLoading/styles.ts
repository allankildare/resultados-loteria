import styled from 'styled-components'

export const Loading = styled.div`
    width: 90px;
    height: 90px;
    border-radius: 50%;
    border: .5rem solid var(--aside-and-megasena-green);
    border-left: .5rem solid transparent;

    animation: rotateLoading .7s infinite ease-in-out;
`
